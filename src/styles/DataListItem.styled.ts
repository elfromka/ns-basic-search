import { css } from "@emotion/react";

// unordered lists item with a data value
const dataListItemStyles = css({
    backgroundColor: "dodgerblue",
    color: "white",
    fontSize: 24,
    padding: "10px",
    border: "1px solid black",
    borderRadius: 4,
});

export { dataListItemStyles };
